{
  "name": "phyllo-connect-web",
  "version": "1.0.0",
  "description": "- Use NPM to install serve. Execute the command `npm install -g serve`.\r - Execute the command `serve` in the root folder of this project to start a HTTP server on your local machine to see this sample app running.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^12.2.1"
  }
}
